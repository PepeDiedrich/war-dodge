# This command runs only after a cheap TCP network check succeeds.
command = echo 'replace this command'
interval = 30m
retry_min = 30s
retry_max = 15m
# A reliable externally reachable host:port. Change this if needed.
health_host = 1.1.1.1:53
connect_timeout = 8s
